# Copy this file to .env and fill in your values
# Default values below assume a local PostgreSQL install with default credentials

DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/ziptrrip_todos?schema=public"
PORT=3000

# Optional — restrict CORS to a specific origin in production
# CORS_ORIGIN=http://localhost:5173
